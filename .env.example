NEXT_PUBLIC_SITE_URL=https://emeraldeyesmiami.com
NEXT_PUBLIC_BUSINESS_PHONE=
NEXT_PUBLIC_BUSINESS_EMAIL=

# Optional: forward every inquiry to a webhook (Zapier, Make, Slack, custom API).
# If omitted, the API will still save a local backup to ../logs/inquiries.ndjson
# when the app runs in a writable environment.
INQUIRY_WEBHOOK_URL=
